{{#section 'head'}}
    <link href="/css/products.css" type="text/css" rel="stylesheet"/>
{{/section}}

<section class="row">
    <div class="col-lg-12">
        <section class="row product-results-container">
            {{#each products}}
                <section class="product-container col-md-6  col-lg-4">
                    <a href="{{ProductLink}}">
                    <section class="product-thumbnail">
                        <img  src="{{ThumbnailUrl}}" alt="thumbnail image"/>   
                    </section>
                    <section class="product-info">
                        <h3>{{Title}}</h3>
                        <div>
                            £{{Price}}
                        </div>
                    </section>
                    </a>
                </section>
            {{/each}}
        </section>
    </div>
</section>
